# 导航部分简述

*本文档写的并非面面俱到，遇到问题来找我就行、或者请ai帮忙*

## 0 目前存在的问题和需要优化的地方

![](\home\ianichinose\gazebo_ws\docs\拟合路径示意.png)

*(导航路径示意)*

### 0.1 问题

1. 上图(看不清可以看`/gazebo_ws/docs/拟合路径示意.png`)中15-17点处转弯有概率撞到墙
2. 20-22部分有概率撞到墙
3. 33-34有概率过不去

### 0.2 解决方向

1. 15-17适度增加导航点
2. 18-21适当增加导航点
3. 33后增加导航点
4. 修改拟合程序```/home/ianichinose/gazebo_ws/src/gazebo_map/scripts/plot_fitted_navigation_path.py```进一步提高光滑度（注意检验是否振荡）

## 1 规划器与代价地图配置文件简述

- 规划器配置文件在```/home/ianichinose/gazebo_ws/src/gazebo_nav/launch/config/move_base```目录下

- 其中```costmap_common_params.yaml```配置全局、局部代价地图的基本参数。**本文件是调整代价地图参数的主要文件** 。

  目前本文件谨慎修改```foot_print``` ```inflation_radius```避免锥桶区实时避障失误

- ```global_costmap_params.yaml```配置全局代价地图、```local_costmap_params.yaml```配置局部代价地图；这两个文件基本没有改的必要

- 局部规划器配置文件在```dwa_local_planner_params.yaml```，本文件是改变、提升机器人导航性能的重点。

  尽量不要调整```yaw_goal_tolerance```和```xy_goal_tolerance```避免震荡，机器人平移速度、加速度，旋转速度、加速度可以适当修改（**但目前已经将```real_time_factor```调到很高，仿真运行速度较快，基本不需要再加快机器人速度**）

- 全局规划器配置文件在```global_planner_params.yaml```基本不需要修改

## 2 运行.launch文件和任务发布

- 仿真任务由```/home/ianichinose/gazebo_ws/src/smart_factory_mission/launch/mission.launch```文件运行

- 任务发布由```/send_navigation_task```发布，当前需要在终端中手动发布

- 综上，可以这样理解，在我们完成了调整，需要启动仿真观察效果时，需要这样运行：

  ```bash
  source ~/.bashrc # 前提是你在bashrc中添加了source指令
  
  roslaunch smart_factory_mission mission.launch
  ```

  等待仿真界面和rviz启动后，另开一个终端运行

  ```bash
  source ~/.bashrc
  
  # task-id 后可以自己改，写什么都行
  # --target-class food 对于导航这一部分不影响，按照默认即可
  rosrun smart_factory_tests send_navigation_task.py \
    --task-id manual_nav_001 \
    --target-class food \
    _server_wait_timeout:=1000.0
  ```

  运行后机器人会开始自动导航

- 如果决定运动过快，可以修改```/home/ianichinose/gazebo_ws/src/car3/world/math.world(line 141)```中的

  ``` xml
  <real_time_factor>0.99</real_time_factor>
  ```

  这个值越小，机器人越慢，亲测改到```0.2```已经很慢

## 3 导航路线形成

#### 3.1 使用了样条拟合获得路径

- 样条拟合基于手动获取的导航坐标点，记录在```/home/ianichinose/gazebo_ws/docs/navi_pos.md```，获取导航点坐标需要启动

  ```bash
  source ~/.bashrc
  
  roslaunch gazebo_nav gazebo_nav.launch
  ```

  这是一个手动导航的.launch文件，待界面稳定后，另开终端

  ```bash
  source ~/.bashrc
  
  rostopic echo /move_base_simple/goal
  ```

  在你正式手动发布坐标之前，它会显示

  ```bash
  WARNING: no messages received and simulated time is active.
  Is /clock being published?
  ```

  现在在rviz中，找到上方```2D Nav Goal```在某处点击并选择朝向后，机器人会开始导航，在发布```roslaunch gazebo_nav gazebo_nav.launch```终端日志报 ```goal reached```后可以在```rostopic echo /move_base_simple/goal```终端得到类似

  ```bash
  header:
    seq: 1
    stamp:
      secs: 2173
      nsecs: 801000000
    frame_id: "map"
  pose:
    position:
      x: -1.651153326034546
      y: -0.8827146291732788
      z: 0.0
    orientation:
      x: 0.0
      y: 0.0
      z: 0.8326031986524846
      w: 0.5538699428508929
  ---
  ```

  的坐标。将其**记录**在```/home/ianichinose/gazebo_ws/docs/navi_pos.md```后可以**让ai计算并写入**```/home/ianichinose/gazebo_ws/src/smart_factory_mission/config/pickup_staging_dev.yaml```。

  写入后可以运行拟合程序

  ```py
  python3 src/gazebo_map/scripts/plot_fitted_navigation_path.py
  ```

  有可能报“栅格占用而不允许生成”，但目前21-22采取直线，所以仅仅此处占用不影响导航，可以查看渲染的上一步同步渲染的当前拟合路径图```/home/ianichinose/gazebo_ws/docs/navigation_path_fit_math_newest.png``` 路径是否与地图墙面重合，判断认为可以作为导航线路后，可以强制输出拟合路径和路径示意图

  ```py
  python3 src/gazebo_map/scripts/plot_fitted_navigation_path.py \
    --allow-unsafe-path-output
  ```

  所以工作流可以这样理解

  ```txt
  打开gazebo_nav.launch
  |
  |
  手动设定导航点并获得坐标
  |
  |
  坐标记录在navi_pos.md
  |
  |
  ai计算并写入正式配置文件pickup_staging_dev.yaml用于拟合
  |
  |
  运行拟合程序获得拟合路径
  |
  |启动mission.launch并发布导航任务验证关键点
  ```

  

## 4 代码实现简述

### 4.1 路径发布

路径发布和任务执行的主要代码在
```/home/ianichinose/gazebo_ws/src/smart_factory_mission/src/smart_factory_mission/mission_server.py```
中，路径几何处理在同目录下的```path_tracker.py```中。

- 任务服务器启动时会读取```mission.yaml```和```pickup_staging_fitted_path.yaml```。

- 当前```route_execution_mode```配置为```fitted_path_lookahead```，因此不会简单地按照离散点逐个停下，而是将拟合结果转换成```nav_msgs/Path```，发布到```/sim_task/reference_path```。

- ```PathTracker```根据```/amcl_pose```和```map -> base_footprint``` TF得到的机器人当前位置，将机器人投影到参考路径上，并且只允许路径进度```s```单调增加，避免机器人在弯道处跳回前面的路径段。

- 路径跟踪目标不是固定不变的终点，而是参考路径前方的一小段```lookahead```目标。曲率较大时减小前视距离，曲率较小时增大前视距离，当前配置范围为```0.20 m```到```0.50 m```。

- 跟踪过程中会发布以下调试话题：

  - ```/sim_task/reference_path```：完整拟合参考路径；
  - ```/sim_task/tracking_goal```：当前发送给```move_base```的前视目标；
  - ```/sim_task/path_progress```：机器人在路径上的累计弧长进度。

- 在```mission.yaml```中可以配置特殊路径段。当前```21 -> 22```配置为直线段，部分弯道可通过```heading_locks```逐渐锁定车头方向，再在到达区段末端前释放，避免急转时车头摆动。

- 拟合路径不是运行时重新计算的。修改```pickup_staging_dev.yaml```中的有效导航点后，需要重新运行```plot_fitted_navigation_path.py```生成新的```pickup_staging_fitted_path.yaml```，再启动任务服务器验证。

路径发布和跟踪过程可以概括为：

```text
pickup_staging_dev.yaml
        |
        |  plot_fitted_navigation_path.py 离线拟合
        v
pickup_staging_fitted_path.yaml
        |
        |  MissionServer 读取并发布
        v
/sim_task/reference_path
        |
        |  PathTracker 根据 AMCL 位姿计算进度和前视点
        v
/sim_task/tracking_goal  --->  move_base
```

### 4.2 任务状态机与定位检查

任务服务器通过```/sim_task/execute```接收```ExecuteTaskAction```。收到任务后，不会立即发送导航目标，而是依次执行：

1. 检查```task_id```和```target_class```是否有效；
2. 等待```/move_base``` Action 服务；
3. 检查```/amcl_pose```是否存在、协方差是否在阈值内；
4. 检查```map -> base_footprint``` TF是否存在且足够新；
5. 读取当前开发阶段的抓取区前置路线；
6. 开始发布参考路径并执行导航。

定位检查使用的是 AMCL 和 TF，不读取```/gazebo/model_states```等 Gazebo 真值。机器人静止时```/amcl_pose```时间戳可能不更新，此时只要实时 TF 仍然有效，可以按照配置允许使用新鲜 TF 作为补充判断。

任务状态会同时发布到```/sim_task/state```，Action 客户端可以从```/sim_task/execute/feedback```观察当前阶段、重试次数和详细信息。

### 4.3 机器人导航执行

```navigation_stage.py```中的```NavigationStage```对标准的```move_base_msgs/MoveBaseAction```进行封装，主要执行逻辑如下：

- 将当前目标封装成```MoveBaseGoal```，发送到```/move_base```；

- 对中间路径点，机器人进入```intermediate_pass_radius```（当前为```0.20 m```）后即可切换到下一个目标，不强制在每个点完全停住；

- 相邻前视目标通过新的```send_goal```直接替换旧目标，利用 Action 的抢占机制连续更新，避免“取消目标—停车—再发送目标”造成额外停顿；

- 只有最终目标要求```move_base```返回```SUCCEEDED```。目标超时、Action 被拒绝、规划失败或任务被取消时，会取消当前目标并进入重试或失败流程；

- 当前单个目标超时时间为```60 s```，最大重试次数为```1```，完整路线超时时间为```240 s```。

部分指定点可以启用航向约束。机器人进入该点的位置范围后，任务服务器先取消当前```move_base```目标，再根据```map -> base_footprint```的当前朝向计算最短角度差，并在```/cmd_vel```发布零线速度、有限角速度的原地旋转命令。航向误差达到容差后发布零速度，再继续下一个导航目标；这部分是任务服务器明确控制的航向对齐阶段，不参与路径规划器的轨迹生成。

因此，完整的机器人控制链路为：

```text
AMCL / TF 提供当前位置
        |
        v
MissionServer / PathTracker 计算前视目标
        |
        v
标准 MoveBaseGoal
        |
        v
move_base + GlobalPlanner + DWAPlannerROS
        |
        v
局部代价地图读取 /scan，输出 /cmd_vel
```

其中路径规划和动态避障仍由```move_base```及其代价地图、DWA 局部规划器完成；任务服务器只负责任务状态、目标更新、超时重试和必要的中间点航向对齐。

## 5 协作流程

见本仓库```README.md```

## 6 本工作空间除基本ROS环境外的依赖

- 本工程按```ROS Noetic + Ubuntu 20.04```环境开发，除了基本 ROS 环境外，导航仿真至少需要以下 ROS 包：

  ```bash
  sudo apt install -y \
    ros-noetic-gazebo-ros-pkgs \
    ros-noetic-gazebo-ros-control \
    ros-noetic-ros-control \
    ros-noetic-ros-controllers \
    ros-noetic-navigation \
    ros-noetic-map-server \
    ros-noetic-amcl \
    ros-noetic-move-base \
    ros-noetic-gmapping \
    ros-noetic-robot-state-publisher \
    ros-noetic-joint-state-publisher-gui \
    ros-noetic-tf \
    ros-noetic-tf2-tools
  ```

- 工作空间编译需要```catkin_make```及其构建工具。若系统尚未安装，可以补充：

  ```bash
  sudo apt install -y build-essential cmake python3-catkin-tools python3-rosdep
  ```

- 路径拟合脚本```src/gazebo_map/scripts/plot_fitted_navigation_path.py```不是 ROS 节点，依赖 Python 3 的以下第三方库：

  - ```numpy```：路线平滑、样条拟合和曲率计算；
  - ```Pillow```：绘制拟合路径示意图；
  - ```PyYAML```：读取和写入路线 YAML 配置。

  Ubuntu 环境可以直接安装对应系统包：

  ```bash
  sudo apt install -y python3-numpy python3-pil python3-yaml
  ```

  也可以使用 pip 安装当前用户环境中的同名包：

  ```bash
  python3 -m pip install --user numpy Pillow PyYAML
  ```

- 任务状态机和单元测试使用 Python 标准库```unittest```；通过 catkin 测试时还需要```python3-nose```：

  ```bash
  sudo apt install -y python3-nose
  ```

- 本工程自带```roboticsgroup_gazebo_plugins```源码包，因此不需要另外下载同名插件。它会在```catkin_make```时编译生成 Gazebo 机械臂关节相关插件。

- 如果只运行已经生成好的导航路线，不需要安装 SciPy；当前拟合程序使用 NumPy 自己实现正则化和自然三次样条计算。只有在修改拟合程序时，才需要额外注意 NumPy、Pillow 和 PyYAML 是否能够正常导入。

- 安装完成后，进入工作空间重新编译并加载环境：

  ```bash
  cd /home/ianichinose/gazebo_ws
  source /opt/ros/noetic/setup.bash
  catkin_make
  source devel/setup.bash
  ```



