# 自动导航任务链路开发记录

记录日期：2026-07-30

## 1. 本次开发范围

本次只搭建完整比赛工程骨架，并实现第一阶段任务：

> 接收任务 → 检查导航与定位状态 → 获取抓取区前置点 → 调用 `move_base` 导航 → 返回到达结果

感知、物块抓取、运输、放置和外部通信暂未实现，只保留对应目录和后续扩展入口。

## 2. 新增 ROS 包

| 包名 | 当前功能 |
| --- | --- |
| `smart_factory_interfaces` | 定义任务 Action 和任务状态消息 |
| `smart_factory_mission` | 导航阶段任务状态机 |
| `smart_factory_bringup` | 仿真、导航和任务节点的统一启动入口 |
| `smart_factory_tests` | 导航阶段测试任务发送器 |
| `smart_factory_bridge` | 外部任务通信占位 |
| `smart_factory_perception` | 目标识别与定位占位 |
| `smart_factory_manipulation` | 机械臂抓取与放置占位 |

原有 `car3`、`gazebo_nav` 和 `gazebo_map` 未修改。

## 3. 已实现的导航任务链路

小车端或测试端通过 `/sim_task/execute` 发送
`smart_factory_interfaces/ExecuteTaskAction`，任务中包含：

- `task_id`：任务唯一标识；
- `target_class`：目标类别，当前支持 `FOOD`、`DAILY`、`ELECTRONICS`。

任务节点依次执行：

1. 检查任务编号和目标类别；
2. 等待 `/move_base` Action 服务；
3. 检查 `/amcl_pose` 是否新鲜、协方差是否在阈值内；
4. 检查 `map → base_footprint` TF；
5. 读取抓取区前置点；
6. 向 `/move_base` 发送标准 `MoveBaseGoal`；
7. 处理成功、超时、失败、重试和任务取消；
8. 成功后返回 `ARRIVED_PICKUP_STAGING`。

任务状态同时发布到 `/sim_task/state`。当前实现不直接发布 `/cmd_vel`，
也不读取 Gazebo 的模型真实坐标。

## 4. 抓取区前置点坐标

坐标配置文件：

```text
src/smart_factory_mission/config/pickup_staging_dev.yaml
```

当前保持未配置状态：

```yaml
pickup_staging:
  configured: false
  frame_id: map
  # x: TODO_MEASURE_IN_RVIZ
  # y: TODO_MEASURE_IN_RVIZ
  # yaw: TODO_MEASURE_IN_RVIZ
```

在 RViz 中用 `2D Nav Goal` 选择经过验证的安全停靠点，可用下面的命令查看目标：

```bash
rostopic echo /move_base_simple/goal
```

记录 `x`、`y` 和航向角 `yaw` 后填入配置文件，并将 `configured` 改为
`true`。`yaw` 的单位为弧度。未完成配置时，任务节点会明确拒绝导航，
不会把缺失坐标当作 `(0, 0)`。

这个固定坐标只用于当前开发阶段，后续比赛版本应由场景配置或感知结果动态提供。

## 5. 启动与测试

编译：

```bash
cd ~/gazebo_ws
catkin_make
source devel/setup.bash
```

启动当前阶段：

```bash
roslaunch smart_factory_bringup nav_to_pickup_stage.launch
```

坐标配置完成后，发送测试任务：

```bash
rosrun smart_factory_tests send_navigation_task.py \
  --task-id nav_demo_001 \
  --target-class food
```

## 6. 验证结果

- `catkin_make` 编译通过；
- Action 与消息生成通过；
- Python 模块和 YAML 配置解析通过；
- `mission.launch` 与 `nav_to_pickup_stage.launch` 解析通过；
- 新增 package.xml 和 launch 文件 XML 语法检查通过；
- 坐标未配置保护检查通过。

## 7. 后续工作

后续可按下面的顺序继续补充：

1. `smart_factory_bridge`：接入比赛任务来源；
2. `smart_factory_perception`：识别物块并输出三维目标位姿；
3. `smart_factory_manipulation`：完成机械臂预抓取、抓取和放置；
4. `smart_factory_mission`：将现有导航阶段扩展为完整比赛状态机；
5. `smart_factory_bringup/full_competition.launch`：启用完整比赛流程。
