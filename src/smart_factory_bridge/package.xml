<?xml version="1.0"?>
<package format="2">
  <name>smart_factory_bridge</name>
  <version>0.1.0</version>
  <description>
    Vehicle-to-simulation bridge. The simulation computer connects as a
    TCP client to the vehicle server (default port 24580), translates the
    NDJSON protocol into /sim_task/execute Action goals, and reports
    truthful ack/progress/result over the link.
  </description>
  <maintainer email="ianichinose@todo.todo">ianichinose</maintainer>
  <license>BSD-3-Clause</license>

  <buildtool_depend>catkin</buildtool_depend>

  <depend>actionlib</depend>
  <depend>rospy</depend>
  <depend>sensor_msgs</depend>
  <depend>std_msgs</depend>
  <depend>smart_factory_interfaces</depend>
</package>
